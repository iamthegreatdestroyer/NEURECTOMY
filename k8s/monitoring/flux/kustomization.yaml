# ============================================================================
# NEURECTOMY - Flux ServiceMonitors for Prometheus
# Metrics collection for Flux CD controllers
# ============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flux-servicemonitors.yaml
  - flux-podmonitor.yaml

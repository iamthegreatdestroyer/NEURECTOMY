# ============================================================================
# NEURECTOMY ML Service - Secrets (Template - DO NOT COMMIT ACTUAL VALUES)
# Replace with sealed-secrets or external-secrets in production
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: ml-service-secrets
  namespace: neurectomy
  labels:
    app.kubernetes.io/name: ml-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: neurectomy-platform
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual values in your deployment pipeline
  # Use sealed-secrets, external-secrets, or vault for production
  database-url: "postgresql://neurectomy:CHANGE_ME@postgres:5432/neurectomy"
  redis-url: "redis://redis:6379"
  jwt-secret: "CHANGE_ME_TO_SECURE_SECRET_IN_PRODUCTION"
